name : shell command

on : [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo string
        run: echo "hello"
      - name: multiline ascript
        run: |
          node -v
          npm -v
      - name: Differnet shell
        run: |
          import platform
          print(platform.processor())

        shell: python 
